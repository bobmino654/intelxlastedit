
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents to logged in users.
    // This is not a secure rule, but it's a good starting point.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
